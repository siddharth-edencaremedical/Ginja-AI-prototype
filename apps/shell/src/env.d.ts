declare module "*.css";

// Injected by Rsbuild `source.define` (see rsbuild.config.ts). These are the
// `remoteEntry.js` URLs the shell registers with the MF runtime on demand.
declare const __PRODUCT_CONFIG_REMOTE_URL__: string;
declare const __UNDERWRITING_REMOTE_URL__: string;
